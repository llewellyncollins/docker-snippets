import { shallowMount } from '@vue/test-utils';

describe( 'SnippetList.vue', () => {
    test( 'TODO', () => {
        expect( true ).toBe( true );
    } );
    /*test( 'should be a Vue instance', () => {
    } )

    test( 'should render correctly', () => {

    } );

    test( 'should not render when not passed props.visible', () => {

    } );

    test( 'should render when passed props.visible', () => {

    } );

    test( 'should show mini view when props.type is not defined', () => {

    } );

    test( 'should show the correct number of snippets', () => {

    } );

    test( 'should show the relevant snippets when searched', () => {

    } );*/
} );
