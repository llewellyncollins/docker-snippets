import { shallowMount } from '@vue/test-utils';

describe( 'Snippet.vue', () => {
    test( 'TODO', () => {
        expect( true ).toBe( true );
    } );
    /*test( 'should be a Vue instance', () => {
    } )

    test( 'should render correctly', () => {

    } );

    test( 'should not render when not passed props.visible', () => {

    } );

    test( 'should render when passed props.visible', () => {

    } );

    test( 'should show the correct snippet information', () => {

    } );

    test( 'should show the correct custom inputs', () => {

    } );

    test( 'should show custom values in the content input', () => {

    } );

    describe( 'onFavoured()', () => {
        test( 'should call onFavoured when the favour button is clicked', () => {

        } );

        test( 'should increment the favourite count by 1', () => {

        } );
    } );

    describe( 'onDownload()', () => {
        test( 'should call onDownload when the download button is clicked', () => {

        } );

        test( 'should increment the download count by 1', () => {

        } );
    } );

    describe( 'onEdit()', () => {
        test( 'should call onEdit when the edit button is clicked', () => {

        } );

        test( 'should change the content input to enabled', () => { } );
    } );

    describe( 'onSave()', () => {
        test( 'should call onSave when the save button is clicked', () => {

        } );

        test( 'should change the content input to disabled', () => { } );
    } );

    describe( 'onDelete()', () => {
        test( 'should call onDelete when the delete button is clicked', () => {

        } );

        test( 'should destroy the component', () => { } );
    } );*/
} );
