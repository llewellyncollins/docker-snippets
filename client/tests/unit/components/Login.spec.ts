import { shallowMount } from '@vue/test-utils';

describe( 'Login.vue', () => {
    test( 'TODO', () => {
        expect( true ).toBe( true );
    } );
    /*test( 'should be a Vue instance', () => {
    } )

    test( 'should render correctly', () => {

    } );

    test( 'should not render when not passed props.visible', () => {

    } );

    test( 'should render when passed props.visible', () => {

    } );

    test( 'should call onLogin when the login button is clicked', () => {

    } );

    test( 'should show successful message after login', () => { } )

    test( 'should show fail message after failed login', () => { } )
    */
} );
