import { shallowMount } from '@vue/test-utils';

describe( 'Home View', () => {
    test( 'TODO', () => {
        expect( true ).toBe( true );
    } );
    /*test( 'should contain a SnippetList.vue', () => {
    } )*/
} );
